
import { createSlice } from '@reduxjs/toolkit'


export  const models = createSlice({
  name: 'models',
  initialState:{
   models:[]
  },
  reducers: {

  },
});

export const { } = models.actions;

export default models.reducer;