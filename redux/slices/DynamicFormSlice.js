import { createSlice } from "@reduxjs/toolkit";

export const DynamicForm=createSlice({
    name:"forms",
    initialState:{
    
    },
    reducers:{
      
    }
})

export const {}=DynamicForm.actions

export default DynamicForm.reducer